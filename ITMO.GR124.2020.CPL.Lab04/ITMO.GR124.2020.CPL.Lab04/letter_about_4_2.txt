Добрый вечер,
цикл крутится потому, что в потоке остались данные (символ конца строки)
и еще важная проблема
- надо передавать параметры по ссылке
- все проверки должны быть только в методе Input, а в мейне только передача нулевых переменных и возврат введенных
- надо учитывать, что cin принимает только соответствующий тип данных, если надо ввести число, а вводится буква, то в поток подается мусор - формально число
вот в таком цикле все работает верно, ввод данных внутри цикла:
        cout << "введите b\n";
        int b01;
        bool dig = false;
        while (dig == false)
        {
                cin >> b01;
                if (b01 == 0)
                {
                        cout << b01 << " 'b'=0, repeat\n";
                }
                else {
                        cout << "digit\n";
                        dig = true;
                }
        }
